# 无尽冬日 MuMu 助手 5.56.0

这是一个基于 Qt 6 的 Windows 桌面工具，通过 MuMu Player 12 的 ADB 截图和模拟点击完成联盟互助与联盟红包页面流程。它不读取游戏内存、不修改 APK，也不会读取账号或密码。

## 冰原巨兽自动集结（v5.56）

1. 在顶部选择一个 MuMu 账号，点击“巨兽”设置该账号的巨兽等级和每日体力消耗上限。默认等级 8；上限 0 表示不限。
2. 进入“巨兽集结”页，点击“开始自动集结巨兽”。需要同时运行多个账号时，使用“全部实例分别启动”，每个账号会在独立窗口读取自己的设置和账本。
3. 助手只走：野外 → 搜索 → 冰原巨兽 → 指定等级 → 集结 → 3分钟 → 发起集结 → 第一编组 → 出征。不会点击自动加入、购买体力、钻石、加速或其他战斗。
4. 最终出征页的实际体力会双帧读取并按本地日期、账号累计。出征前记录账号“野外”行军面板中每一行的空闲/占用状态；出征后必须唯一确认一行由空闲变占用才记账，并且只跟踪这一行连续两次恢复空闲后进入下一轮。采矿或其他既有行军不会阻塞仍有空闲行的账号。

所有识图输入都需要两张实时新截图；动态轮询不超过 1.5 秒，单个动作状态不超过 30 秒。未知页面会停止，不会盲点返回或关闭。

## 运行要求

- Windows 10/11 64 位
- MuMu Player 12
- 已安装并启动《无尽冬日》，且 MuMu 已启用 ADB

## 启动与多开

1. 启动 MuMu Player 和游戏。
2. 运行 `WJDRMuMuAssistant.exe`，或从源码运行 `wjdr_mumu_assistant_qt.py`。
3. 使用顶部的实例选择器选择目标 MuMu 实例；程序会显示实例编号、独立 ADB 地址、分辨率和游戏前台状态。
4. 多开时，每个助手窗口固定绑定一个实例。不同实例可以同时运行；同一 Android 实例即使以不同 ADB 地址出现，也会共用跨进程占用保护，避免重复点击。

按 **F8** 可停止所有正在运行的任务。

## 自动导航并点击“全部帮助”

在“联盟帮助”页点击 **自动导航并全部帮助**。程序每一轮都会重新识别当前页面：

1. 主城 / 世界地图：寻找并点击联盟入口。
2. 联盟主页：寻找并点击联盟互助入口。
3. 联盟互助页：同时确认页面标题与底部绿色“全部帮助”按钮。
4. 仅在第 3 步确认后，记录该次截图中的按钮中心并点击。
5. 点击后立即重新截图验证；按钮消失或页面不再匹配时，会丢弃旧坐标并停止点击。

循环间隔由界面中的“点击间隔”控制。建议从较长间隔开始，确认表现符合预期后再调整。

## 自动抢“熔炉升级红包”

在“联盟红包”页点击 **开始自动抢红包**。该功能只处理联盟聊天里的 **熔炉升级红包**，并按下面的视觉确认链路工作：

1. 连续两次在主城右下区域看到红包浮标，才会开始检查；浮标本身不会触发领取。
2. 只有程序自己点击过主城聊天入口、并在后续截图确认聊天面板后，才会点击“联盟”标签；在此前红包浮标消失会立即复位且不输入。
3. 必须同时确认聊天面板、“联盟”已选中、“熔炉升级红包”标题、未领取红包图标及其相对位置，才会打开红包卡。
4. 详情中必须同时识别“熔炉升级红包”标题与圆形“开启”按钮，且两者位置关系符合实测布局，才会点击一次。
5. 点击后必须再次识别奖励结果页和该结果页关闭按钮，才计为成功、关闭结果并回到主城继续监听。

聊天记录不会自动滚动。目标卡不在当前画面、红包已领完、页面未知、结果未确认或游戏不在前台时，程序不会点击；识别到“已领取”卡会安全返回主城继续监听，不会打开该卡。

可在该页单独设置检查间隔、运行时长和最多领取次数；“全部实例抢红包”会为每个 MuMu 实例打开一个绑定窗口。也可双击 `启动联盟红包挂机.cmd`，或使用命令行 `WJDRMuMuAssistant.exe --all-auto-red-packet`。

同一模拟器实例一次只能运行一个自动任务。联盟帮助与联盟红包同时启动时，设备锁会拒绝后启动的窗口，避免两个流程争夺页面。

## 每日任务自动化

在“每日任务”页点击 **开始每日任务自动化**，或运行 `WJDRMuMuAssistant.exe --auto-daily`。多个 MuMu 实例可在界面中点击“全部实例执行每日任务”，也可以运行 `WJDRMuMuAssistant.exe --all-auto-daily`，以及随包的 `启动每日任务挂机.cmd`、`启动全部MuMu实例每日任务.cmd`。若启动时已在每日任务页（包括显示绿色“领取”的任务页），程序会双帧确认该页后仅点击该页关闭按钮，回到主城完成大熔炉等级预检；不会用通用返回或点击任务“前往”。

流程会收取连续两帧确认的任务奖励与已达标活跃度宝箱；从主城打开任务页时，若游戏默认显示“章节任务”，会在连续确认章节标题和未选中“每日任务”标签后切换一次。任务列表只在已验证的 `x=900` 列表通道滑动，每次都复核任务区是否真实移动；连续两次无位移才确认顶部或底部，最多 12 次仍无法确认就安全停止。每个新视口都会查找已精确识别的训练、建筑、采集、普通联盟捐献、免费英雄招募与竞技场任务。竞技场从“进行1次竞技场挑战”的同排“前往”进入后，会连续完成所有可证明的普通免费挑战，免费次数为0才回每日页领取1次和5次两档；不点刷新、加号或付费次数。若完整扫描后仍未达到 325 分，所有失败/让行重试最多锁 30 秒，仓库3分钟、免费招募5分钟和自然队列倒计时保留为业务边界。达到 325 分必须经连续两帧确认；此后禁止任何新增进度路线，但从双确认顶部继续只领取扫描，排空精确行领取/绿色一键领取及其关联结果后才停止，最终分数允许超过 325。

盾兵任务会先双帧确认普通训练页与右侧数量框；只有 Android 同时证明输入法已显示、连接为可编辑且服务对象是 Unity 游戏视图时，才会直接清空数量框并输入 **10**。提交后还要连续两帧复核为 10，才点击蓝色“训练”。队列启动后会双帧识别并保存一次无账号倒计时裁剪，随后返回每日任务继续领取、联盟帮助和其他不占训练队列的事项；八分钟内不会再进入任何训练卡。黄色“立即完成”和蓝色“加速”均不属于自动化候选按钮。

资源任务仅允许经过验证的“前往 → 世界搜索 → 对应资源分类 → 搜索 → 采集 → 出征”。生肉任务会由精确的每日卡片“前往”预选生肉，程序仅在资源轮播中央“生肉”标签二次确认后搜索，不猜测资源轮播坐标。每个控制点均连续两帧确认；出征后只被动观察“行军 1/1”是否先连续可见、后连续消失，以确认队伍自然返回后再开始下一轮，不会固定空等或使用加速。只使用普通资源与免费出征。建筑任务只允许“已验证前往 → 建筑信息页 → 下方普通蓝色建造”，随后轮询自然完成，绝不使用黄色立即完成、蓝色加速或钻石。对于“完成联盟捐献”任务，程序只会点击蓝色普通食物捐献，绝不点击黄色钻石捐献；每批固定 5 次、每次都必须由联盟币计数增长确认，下一批还要重新看到同一卡片“前往”，单进程最多 4 批。普通粮食按钮变灰或联盟币不增长后，本进程会跳过后续捐献卡并继续独立任务。4.82 只在完整“发展/领地/战斗”标签栏及战斗选中态连续两帧成立后扫描战斗科技树；先确认顶部，再逐视口向下查找精确“联盟永续”标题，每次滑动均比较像素并复核标签。普通训练数量框必须与精确训练页同帧双重确认；输入法焦点证明失败时不发送任何按键，输入后仍需双帧确认 10。训练完成教程仍需盾兵营、手势目标和中央肤色三重证据，但只点击不随动画漂移的固定黄色圆心。精确招募页若确认没有绿色免费控件，本进程会跳过后续英雄招募卡并继续独立任务，绝不点击钥匙或钻石。训练卡“前往”若没有离开每日页，只在再次被动双帧确认仍为每日页后跳过训练、继续独立任务，不点击任何恢复控件；落入未知页仍立即停止。程序不会点击钻石、钥匙、加速、购买、战斗、非任务路由的升级或任意未知弹窗。未知页、付费礼包、任何已识别的遮挡/变暗画面或坐标不稳定时，都会不输入并停止。

## 分辨率兼容性

内置模板以实际截图为参考，运行时会按当前游戏内容区进行多尺度匹配和坐标换算。联盟红包流程已在真实 1440×2560 MuMu 画面完成“浮标 → 聊天 → 联盟 → 熔炉红包 → 开启 → 奖励结果”回读；并对 720×1280、768×1365、900×1600、960×1707、1080×1920、1200×2133、1280×2276 和 1440×2560 做了截图缩放回归。MuMu 窗口出现上下或左右纯黑边时，会先检测中间游戏内容区再换算坐标。

如果游戏更新导致识别失败，可在界面中刷新截图并框选新按钮保存为模板，再使用“识图点击”模式测试。

## 风险提示

自动化、宏或机器人可能违反游戏服务条款，并可能带来账号处罚风险。请仅在自行承担风险的前提下短时、有人监看的测试；不要用于战斗、交易、充值、抢占资源或批量账号操作。

运行日志、模板和本地配置保存在：`%LOCALAPPDATA%\WJDRMuMuAssistant`。
