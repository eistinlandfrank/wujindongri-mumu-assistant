# WJDR 快速识图工具

`wjdr_fast_vision.py` 是独立的只读排查工具。它把一张截图只转换一次，启动时一次性加载模板，并缓存当前分辨率的缩放结果；同一帧上的模板匹配并行执行。ADB 模式只有 `screencap` 路径，不包含点击、滑动、返回或按键命令。

v5.11 便携包会同时附带 `快速识图.cmd`、`wjdr_fast_vision.py`、`wjdr_backend.py` 和 `requirements.txt`；专用行军/编队数字识别器也在本地进程内运行，不依赖通用 OCR。该诊断工具使用系统 Python，不会启动或替换正在运行的自动化进程。

## 常用命令

从历史截图快速判断联盟科技标签：

```powershell
.\快速识图.cmd inspect `
  --image .\evidence\milestone-09-alliance-tech-scan\territory-00-private.png `
  --pattern "daily_alliance_tech_.*tab_strip" `
  --top 10
```

MuMu 的 `16416` 在线时，只取一张新鲜画面并批量识别全部每日模板：

```powershell
.\快速识图.cmd inspect `
  --device 127.0.0.1:16416 `
  --pattern "^daily_" `
  --top 20 `
  --json-out .\evidence\fast-current.json `
  --safe-crop-out .\evidence\fast-current-safe.png
```

只排查用户 Word 文档沉淀的仓库、情报和竞技场模板：

```powershell
.\快速识图.cmd inspect `
  --device 127.0.0.1:16416 `
  --pattern "^user_doc_" `
  --top 10
```

当前 7 张紧凑文档模板在 `1440×2560` 历史每日页上的批量实测约为 `0.17 秒`。其中竞技场和情报匹配仅用于识别/跳过，不能授权战斗或任意地图节点。

批量检查一个证据目录：

```powershell
.\快速识图.cmd corpus `
  --input-dir .\evidence\milestone-09-alliance-tech-scan `
  --glob "*.png" `
  --pattern "daily_alliance_(tech|sustain)" `
  --json-out .\evidence\milestone-09-alliance-tech-scan\corpus.json
```

检查两次科技树滑动是否真的移动：

```powershell
.\快速识图.cmd diff `
  .\evidence\milestone-09-alliance-tech-scan\viewport-02-safe.png `
  .\evidence\milestone-09-alliance-tech-scan\viewport-03-safe.png
```

## 选项

- `--pattern`：正则筛选模板；排查一个功能时应尽量缩小范围。
- `--mode fast`：每张模板只使用由内容视口推导出的精确缩放，适合固定 MuMu 分辨率，也是默认值。
- `--mode robust`：使用与发行助手相近的多尺度容差，适合来源不确定的截图，但速度更慢。
- `--region left,top,right,bottom`：只搜索指定矩形，可显著降低耗时和误匹配。
- `--threshold`：默认 `0.94`。本工具给出的 `hit` 只是排查候选，不授权游戏点击；实际自动化仍必须经过页面、颜色完整性、双帧和任务关联检查。
- `--annotated-out`：保存完整标注图，可能包含账号信息，应视为私有证据。
- `--safe-crop-out`：只保存默认中央脱敏区域；如页面中央仍有聊天或名称，应通过 `--safe-relative-box` 收窄。

## 性能原则

旧流程对每张模板重复进行磁盘读取、整屏灰度转换和视口计算。快速工具将这些固定成本提升到“每个进程一次”或“每帧一次”，所以一张图匹配几十张模板时收益最大。

本机对同一张 `1440×2560` 截图批量匹配 72 个每日模板的实测结果：

- 快速工具：`827.921 ms`
- 旧逐模板流程：`13,494.705 ms`
- 提速：`16.3×`
- 新旧流程第一候选均为 `daily_alliance_tech_territory_tab_strip`，分数均为 `1.0`

测试代码位于 `tests/test_fast_vision.py`，完整机器基准位于 `evidence/milestone-09-alliance-tech-scan/fast-vision-benchmark.json`。

## 安全边界

- 工具只截图、读图、比对和写本地报告，没有 `tap`、`swipe`、`keyevent` 或其他游戏输入路径。
- `hit` 只用于快速定位候选，不可直接授权点击；发行助手仍需执行原有双帧、页面、颜色和任务关联校验。
- `fast` 模式用于固定 MuMu 分辨率；画面来源或缩放不确定时改用 `robust`，不要为了速度降低阈值。
- 已在 MuMu `127.0.0.1:16416` 上完成实时只读验证：设备报告为 `device`，1440×2560 实时画面批量匹配 72 个模板耗时 `891.899 ms`，正确识别主城入口，分数 `0.986372`。
