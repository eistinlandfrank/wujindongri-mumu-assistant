# 兼容性测试报告（4.13.0）

## 测试范围

- 平台：Windows 10/11、MuMu Player 12、ADB 截图与点击通道。
- 联盟帮助流程：主城 / 世界地图 → 联盟主页 → 联盟互助 → 全部帮助。
- 联盟红包流程：红包浮标 → 主城聊天入口 → 联盟频道 → 熔炉升级红包 → 开启 → 奖励结果页回读。
- 多开模式：不同 MuMu 实例可分别启动；同一 Android 实例的多 ADB 别名去重与重复占用拦截。

## 分辨率回归

| 游戏内容区 | 页面状态识别 | “全部帮助”模板匹配 |
| --- | --- | --- |
| 720×1280 | 通过 | 通过 |
| 900×1600 | 通过 | 通过 |
| 1080×1920 | 通过 | 通过 |
| 1200×1920 | 通过 | 通过 |
| 1080×2160 | 通过 | 通过 |
| 1080×2400 | 通过 | 通过 |
| 1440×2560 | 通过 | 通过 |
| 1600×2560 | 通过（左右黑边内容区换算） | 通过 |

### 联盟红包模板缩放回归

下表记录内置模板的缩放回归范围；它验证页面锚点在对应内容区尺寸下的匹配与坐标换算。游戏或 MuMu 更新后仍应先以人工观察方式确认，再开启自动操作。

| 游戏内容区 | 红包浮标 | 联盟目标卡 | 开启 / 结果页 |
| --- | --- | --- | --- |
| 720×1280 | 已纳入缩放回归 | 已纳入缩放回归 | 已纳入缩放回归 |
| 768×1365 | 已纳入缩放回归 | 已纳入缩放回归 | 已纳入缩放回归 |
| 900×1600 | 已纳入缩放回归 | 已纳入缩放回归 | 已纳入缩放回归 |
| 960×1707 | 已纳入缩放回归 | 已纳入缩放回归 | 已纳入缩放回归 |
| 1080×1920 | 已纳入缩放回归 | 已纳入缩放回归 | 已纳入缩放回归 |
| 1200×2133 | 已纳入缩放回归 | 已纳入缩放回归 | 已纳入缩放回归 |
| 1280×2276 | 已纳入缩放回归 | 已纳入缩放回归 | 已纳入缩放回归 |
| 1440×2560 | 模板基准尺寸 | 模板基准尺寸 | 模板基准尺寸 |

### 黑边与安全状态机回归

- 已以 1080×2400、1080×2160 上下黑边、1600×2560 左右黑边及上下各 540px 的极端黑边合成截图验证内容区坐标换算；聊天入口、联盟页签和卡片/详情几何均以内容区为准。
- 连续两帧浮标后若浮标在首次聊天点击前消失，仿真回归为零输入。
- 预先打开的聊天面板不会绕过“本流程已点击聊天入口”的前置条件。
- “已领取”熔炉红包被归为无点击终态；自领结果页先变为“已领取”时仍只执行一次安全返回。
- `FURNACE_PACKET → UNKNOWN → FURNACE_PACKET` 抖动回归中，卡片点击严格封顶为两次，不会重置计数。
- 对 50 张非红包历史游戏截图扫描，未出现可执行的红包卡、开启或结果关闭状态。

## 每日任务与普通采集回归

每日任务流程会领取已完成任务奖励与活跃度宝箱，并处理实机确认过的生肉、木材、煤炭、铁矿普通采集任务、“训练10个盾兵（0/10）”和“升级1次建筑（0/1）”。其中生肉路线已在实机依次验证“前往 → 生肉预选 → 搜索 → 废弃畜牧场 → 采集 → 出征”；资源轮播中央“生肉”标签必须通过原始 RGB 完整性校验，5% 变暗时不产生搜索输入。以下用实测页面截图、缩放画布和只读/假设备回归完成；除明确列出的受限动作外，其他状态均没有可执行坐标。

| 盾兵普通训练面板 | 同卡任务、入口、蓝色训练和数量10均连续复核 | 仅允许蓝色普通训练；黄色立即完成与蓝色加速无坐标 |
| 盾兵训练完成 | 训练中状态、返回兵营、兵营结构连续复核 | 只在自然完成等待后点击兵营收取；未复核则停止 |
| 建筑升级任务 | 精确“升级1次建筑（0/1）”任务、同卡前往、建筑信息标题和下方普通蓝色建造均连续复核 | 只启动一次普通建造并轮询自然完成；钻石完成、加速与非任务升级无坐标 |
| 居中活跃度宝箱结果页 | 顶部指针锚点与已点击宝箱的同轮关联 | 只关闭已关联结果页；普通每日页无关闭坐标 |

| 页面或保护场景 | 识别结果 | 输入结论 |
| --- | --- | --- |
| 主城任务入口（含两组不同主城画面） | `CITY_ENTRY` | 仅在连续两帧、且本轮尚未导航时允许一次入口点击 |
| 每日登录（1/1）可领取 | `CLAIM_READY` | 仅同一行绿色“领取”可点击一次 |
| 领取动画结束后的每日任务页 | `DAILY_PAGE` | 无坐标；复核两帧后计数并停止 |
| 已完成/无领取按钮 | `DAILY_LOGIN_COMPLETED` 或 `DAILY_PAGE` | 零输入停止 |
| 实测付费新人礼包 | `BLOCKED` | 零输入停止，不关闭、不返回 |
| 未知页面、红包页面、主题活动页 | `UNKNOWN` | 零输入停止 |
| 章节任务默认页 | `DAILY_TAB_READY` | 章节标题和未选中“每日任务”标签连续两帧确认后，允许一次标签切换 |
| 生肉 / 木材 / 煤炭 / 铁矿任务卡 | `DailyMissionMatch` | 任务标题与同一卡片“前往”同帧成立，且按钮完整性通过后才可进入资源路线；相似资源标题采用 0.96 精确匹配门槛 |
| 生肉预选资源轮播 | 居中“生肉”标签 + 原始 RGB 完整性 | 仅确认任务页“前往”已预选生肉后才可搜索；不点击或猜测轮播坐标，5% 变暗零输入 |
| 新号世界搜索引导 | 世界搜索入口 | 普通图标或引导手势仅在本流程已点击对应任务“前往”后可触发一次搜索入口点击 |
| 资源点采集 / 出征 | 普通资源操作 | 连续两帧确认；只点击普通资源按钮，不使用钻石、钥匙或加速 |

每日素材已在 `720×1280`、`768×1365`、`900×1600`、`960×1707`、`1080×1920`、`1200×2133`、`1280×2276`、`1440×2560` 做缩放回归；`1080×2400` 和上下各 540px 的极端黑边画布仍按游戏内容区映射坐标。

为避免半透明覆盖层被归一化模板相关性误判，入口、领取、任务“前往”、采集和出征会做原始 RGB 亮度/颜色完整性校验；5% 及以上全屏变暗合成回归、付费弹窗及遮挡合成回归都不会产生输入状态。领取、宝箱、标签切换、任务入口和资源路线坐标均需连续两帧稳定；任务列表上滑最多 4 次，普通采集会等待队伍返回后才开始下一轮。

## 安全行为检查

- 仅在联盟互助标题和绿色“全部帮助”按钮同时命中时触发点击。
- 点击后立即刷新截图；进入空页面或按钮消失时不再使用旧坐标。
- 未命中模板、游戏不在前台或设备断开时，任务会暂停而不是盲点。
- 多开时，窗口日志和任务绑定到各自的 Android 目标；本机实测 `127.0.0.1:16384`、`127.0.0.1:7555`、`emulator-5554` 三个别名会归并为同一 Android ID，并由同一占用锁拦截。
- 红包浮标仅作为连续两帧的上游触发；在程序实际发送主城聊天点击前，每帧仍需看见浮标。领取前还需逐帧确认联盟频道、未领取目标卡、详情标题和开启按钮。
- “开启”只允许一次；只有奖励结果锚点和结果页关闭按钮同帧命中，才会计为领取并关闭结果页。
- 未采到目标卡、聊天频道未确认、目标已领完、页面未知或开启后未回读结果时，不会继续输入；已领取卡只允许单次安全返回。
- F8 可全局停止运行中的任务。

## 发布前复核

- 源码可通过 Python 语法编译检查。
- 发布包不包含账号名、角色名、联盟名、服务器编号、设备序列号、个人目录或本机测试日志。
- 本报告仅保留可复现的通用测试结论，不包含个人游戏数据。
